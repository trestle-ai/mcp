# @trestle/mcp

MCP step execution for Trestle workflows. Bundled into `@trestle/sdk` at publish time — do not install directly for app use.

Forked from `@relayplane/mcp` as part of RelayPlane excision.
